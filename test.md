  1단계: PDF에서 수평선 찾기

  drawings = page.get_drawings()  # PDF의 모든 그래픽 객체

  for item in d.get("items", []):
      if item[0] == "l":  # "l" = line (선)
          p1, p2 = item[1], item[2]  # 시작점, 끝점

          # 수평선인지 확인 (y좌표가 거의 같으면 수평)
          if abs(p1.y - p2.y) < 2:
              # 길이가 10~500px인 선만 저장

  PDF 내부 구조:
  ┌────────────────────────────────┐
  │  텍스트: "jewellery"           │
  │  ══════════════════  ← 선 객체 │
  │  (x0=100, y=200, x1=180)       │
  └────────────────────────────────┘

  2단계: 선 위의 텍스트 추출

  # 선 좌표 기준으로 사각형 영역 만들기
  rect = fitz.Rect(
      line["x0"] - 1,      # 왼쪽
      line["y"] - 12,      # 위 (선 위로 12px)
      line["x1"] + 1,      # 오른쪽
      line["y"] + 1        # 아래
  )

  # 해당 영역의 텍스트만 추출
  text = page.get_text("text", clip=rect)

  시각적 설명:
          x0=100          x1=180
             │              │
      ┌──────┼──────────────┼──────┐ ← y-12
      │      │  jewellery   │      │
      │      │              │      │
      └──────┼──────────────┼──────┘ ← y+1
             ═══════════════════════  ← 밑줄 선 (y=200)

  흐름도

  PDF 열기
      ↓
  페이지마다 반복
      ↓
  get_drawings()로 모든 선 추출
      ↓
  수평선만 필터링 (y좌표 차이 < 2)
      ↓
  각 선의 좌표 (x0, x1, y) 저장
      ↓
  선 바로 위 영역(rect) 생성
      ↓
  get_text(clip=rect)로 해당 영역 텍스트 추출
      ↓
  중복 제거 후 결과 반환

  핵심 원리

  KIPO 통지서의 밑줄은 텍스트 서식이 아닌 별도의 그래픽 선으로 그려져 있음. 그래서 선의 위치를 찾고 → 그 위에 있는 텍스트를 추출하는 방식.
